- name: clone infra ansible repo
  ansible.builtin.git:
    repo: 'https://github.com/MaHi-312/infra-ansible'
    dest: /tmp/infra-ansible

- name: run shell command
  shell: pwd
  register: out

- name: debug
  debug:
    msg: "{{ out }}"